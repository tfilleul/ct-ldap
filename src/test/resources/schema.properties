AccountExpirationDate;one
accountExpires;one
AccountLockoutTime;one
AccountNotDelegated;one
AllowReversiblePasswordEncryption;one
BadLogonCount;one
badPasswordTime;one
badPwdCount;one
businessCategory;multi
c;one
CannotChangePassword;one
CanonicalName;one
Certificates;multi
City;one
CN;one
co;one
codePage;one
Company;one
Country;one
countryCode;one
Created;one
createTimeStamp;one
Deleted;one
Department;one
Description;one
directReports;multi
DisplayName;one
DistinguishedName;one
Division;one
distingName;one
DoesNotRequirePreAuth;one
dSCorePropagationData;multi
EmailAddress;one
EmployeeID;one
EmployeeNumber;one
Enabled;one
extensionAttribute1;one
extensionAttribute10;one
extensionAttribute2;one
extensionAttribute3;one
extensionAttribute4;one
extensionAttribute5;one
extensionAttribute6;one
extensionAttribute7;one
extensionAttribute8;one
extensionAttribute9;one
Fax;one
GivenName;one
HomeDirectory;one
HomedirRequired;one
HomeDrive;one
homeMDB;one
homeMTA;one
HomePage;one
HomePhone;one
Initials;one
instanceType;one
isDeleted;one
l;one
LastBadPasswordAttempt;one
LastKnownParent;one
lastLogoff;one
lastLogon;one
LastLogonDate;one
lastLogonTimestamp;one
legacyExchangeDN;one
LockedOut;one
lockoutTime;one
logonCount;one
logonHours;multi
LogonWorkstations;one
mail;one
mailNickname;one
Manager;one
mAPIRecipient;one
mDBUseDefaults;one
MemberOf;multi
MNSLogonAccount;one
MobilePhone;one
Modified;one
modifyTimeStamp;one
msDS-User-Account-Control-Computed;one
msExchHomeServerName;one
msExchMailboxGuid;multi
msExchMailboxSecurityDescriptor;one
msExchMobileMailboxFlags;one
msExchPoliciesExcluded;one
msExchRecipientDisplayType;one
msExchRecipientTypeDetails;one
msExchUserAccountControl;one
msExchUserCulture;one
msExchVersion;one
msNPAllowDialin;one
msRTCSIP-DeploymentLocator;one
msRTCSIP-FederationEnabled;one
msRTCSIP-InternetAccessEnabled;one
msRTCSIP-OptionFlags;one
msRTCSIP-PrimaryHomeServer;one
msRTCSIP-PrimaryUserAddress;one
msRTCSIP-UserEnabled;one
msRTCSIP-UserPolicies;multi
Name;one
nTSecurityDescriptor;one
ObjectCategory;one
ObjectClass;multi
ObjectGUID;one
objectSid;one
Office;one
OfficePhone;one
Organization;one
OtherName;one
PasswordExpired;one
PasswordLastSet;one
PasswordNeverExpires;one
PasswordNotRequired;one
POBox;one
PostalCode;one
postOfficeBox;multi
PrimaryGroup;one
primaryGroupID;one
ProfilePath;one
ProtectedFromAccidentalDeletion;one
proxyAddresses;multi
publicDelegatesBL;one
pwdLastSet;one
SamAccountName;one
sAMAccountType;one
ScriptPath;one
sDRightsEffective;one
ServicePrincipalNames;multi
showInAddressBook;multi
SID;one
SIDHistory;multi
SmartcardLogonRequired;one
sn;one
st;one
State;one
StreetAddress;one
Surname;one
telephoneNumber;one
textEncodedORAddress;one
Title;one
TrustedForDelegation;one
TrustedToAuthForDelegation;one
UseDESKeyOnly;one
userAccountControl;one
userCertificate;one
UserPrincipalName;one
uSNChanged;one
uSNCreated;one
whenChanged;one
whenCreated;one